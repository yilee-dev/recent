server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fsrv
    username: root
    password: yiLee839195!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  servlet:
    multipart:
      max-file-size: 2GB
      max-request-size: 4GB

  messages:
    basename: messages,errors

  devtools:
    restart:
      enabled: false

file:
  store:
    root: "C:/fsrv"
